<Page
    x:Class="Client.Pages.UsageChoicePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>
        
        <TextBlock
            x:Name="Title"
            HorizontalAlignment="Center"
            Text="How is this device using PassiveEyes?"
            VerticalAlignment="Center" />

        <Button
            x:Name="RecorderChooser"
            Click="RecorderChooser_Click"
            Content="As a recorder"
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch" />

        <Button
            x:Name="ControllerChooser"
            Click="ControllerChooser_Click"
            Content="As a controller"
            Grid.Row="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch" />
    </Grid>
</Page>
